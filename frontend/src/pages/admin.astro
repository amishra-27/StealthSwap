---
/*
Docs references:
- Astro islands (`client:load`): https://docs.astro.build/en/concepts/islands/
- Uniswap v4 hooks concept (pool-scoped hook execution): https://docs.uniswap.org/contracts/v4/concepts/hooks
- Uniswap v4 PoolManager lifecycle overview: https://docs.uniswap.org/contracts/v4/overview
- Uniswap v4 AsyncSwap/custom accounting context: https://docs.uniswap.org/contracts/v4/quickstart/hooks/async-swap
- OpenZeppelin Uniswap Hooks base contracts: not used in this frontend page.
*/

import AdminClearPanel from "../components/AdminClearPanel.vue";
import AppShell from "../layouts/AppShell.astro";
---

<AppShell
  title="Executor Console - Manual Clear"
  subtitle="Fallback control surface for demos when automated clear execution is offline."
  pageId="admin"
>
  <section class="workflow-layout">
    <section class="workflow-main">
      <article class="glass-card">
        <div class="section-row">
          <h2 class="section-title">Clear Window</h2>
          <span class="chip chip-warning">Executor Gated</span>
        </div>
        <p class="section-note">
          Manual clear for previous ended window. This path is permissionless onchain but UI-gated
          by configured executor address.
        </p>
        <AdminClearPanel client:load />
      </article>
    </section>

    <aside class="secondary-panel">
      <article class="glass-card">
        <h3 class="section-title">Safety Notes</h3>
        <ul class="bullet-list">
          <li>Use the same executor wallet as `PUBLIC_EXECUTOR_ADDRESS`.</li>
          <li>Confirm target network is Sepolia before signing.</li>
          <li>Validate previous window is ended and not already cleared.</li>
        </ul>
      </article>
    </aside>
  </section>
</AppShell>
